# Results directory

This directory contains:

- starter tables generated from the included raw files,
- starter figures generated from the included raw files,
- baseline event-study text output where enough seeded data exist.

These are not the final research results. They are smoke-test outputs showing that the scaffold already runs.

Current build notes:
- ACS rent-profile coverage in this workspace is 2010-2019 and 2021-2024. The 2020 ACS 1-year profile endpoint remains unavailable, and the rent/burden series now use year-aware harmonization because official profile codes shift between 2010-2014 and 2015+.
- ACS mobility variables are harmonized year-aware across 2015-2018 and 2019+ because the official profile variable codes shift between those vintages.
- QCEW core outputs in this workspace now cover the selected states for 2014-2024, which is the available range from the official QCEW area-slice API used here.
- Credibility artifacts now include placebo, donor-sensitivity, leave-one-donor-out, CA-only / OR-only, state-interaction, and quarterly treatment-definition sensitivity outputs under `results/tables/` and `results/figures/`.
- A concise interpretation of the current credibility outputs lives in `results/credibility_interpretation.md`.
- A quarterly-focused reporting package lives in `results/quarterly_reporting.md` with companion figures and summary tables.
- A free-data extension package now lives in `results/free_data_extensions.md` with companion tables that map research questions `Q1-Q10` to concrete artifacts and summarize treated-state contrasts for the remaining heterogeneity questions.
- A local NYC package now lives in `results/nyc_local_interpretation.md` with companion match summaries, borough-year comparison tables, and one public comparison figure for RSBL-listed versus non-listed HPD-observed buildings.
- The NYC local package also now includes a richer `MapPLUTO + MDR` stratified comparison layer with registration-backed controls and community-board/building-type strata.
- The NYC local package also now includes two within-community-board matched layers: the first prototype and a refined risk-aware matcher. The refined matcher materially improves pre-period balance, but it also attenuates the treated-control gap, so the local NYC read is now explicitly design-sensitive rather than one-directional.
- The NYC local package now also includes a building-level fixed-effects event model on the enriched balanced panel plus a feasible within-block matched prototype. The FE model preserves the broader positive differential-growth pattern, while the within-block matcher does not improve balance relative to the refined community-board matcher.
- The NYC local package now also includes a follow-on question-shortlist layer with treated-stock selection coverage, borough/community-board gap concentration, building-type heterogeneity, extensive-versus-intensive margin tables, and a backend-extension manifest for monthly timing and status-aware HPD summaries.
- The NYC follow-on layer now also includes a narrowed live 2025 extension: registration-lifecycle splits plus grouped HPD monthly timing and status-family tables for Questions 5 and 6.
- Los Angeles now has a bounded public `Property Activity Report` pilot that confirms a machine-usable LAHD surface with `APN`, address, rent-registration number, unit count, year built, and case-history fields from public address queries.
- Los Angeles now also has a materially larger bounded comparative pilot across `8` fixed street queries (`MAIN`, `BROADWAY`, `SPRING`, `OLYMPIC`, `FIGUEROA`, `VERMONT`, `WESTERN`, `ALVARADO`). The current saved tranche keeps `150` deduped properties, `84` properties with rent registration numbers, `150` properties with case history, and `2,367` total case rows. New public-facing outputs now include `los_angeles_query_coverage_summary.csv`, `los_angeles_sample_strata_summary.csv`, and `results/los_angeles_public_reporting_note.md`.
- Los Angeles still keeps the official LA County Assessor context layer under `los_angeles_assessor_*`. Citywide 2025 parcel counts remain reproducible from the official ArcGIS service, but the expanded sample-level assessor join is still limited by live endpoint timeouts, so the current saved sample-level assessor matches remain sparse (`2` matched parcels) and should be treated as context rather than a complete parcel join.
- West Hollywood now has a parser and runnable protected-stock pilot for the official rent-stabilized address list. Automated retrieval is still blocked by `403` from the city's Akamai-protected document server, but the manual `--pdf-path` fallback has been run successfully and produced `17,175` parsed rows, `5,987` unique addresses, and `2,108` unique parcels.
- West Hollywood now also has a reporting-ready second-stage linkage package. Using a bounded 2025 archive of official Rent Stabilization Commission minutes plus two official Socrata datasets, the backend links the protected-stock table to `34` 2025 appeal rows (`14` matched to RSO stock), `206` buyout records (`71` matched to RSO stock), and `851` seismic retrofit parcel rows (`705` matched rows representing `688` normalized stock addresses / `689` unique RSO parcels`). The public-facing denominator and footprint tables now live in `weho_surface_match_rates.csv`, `weho_stock_denominator_summary.csv`, `weho_buyout_footprint_summary.csv`, `weho_seismic_footprint_summary.csv`, `weho_appeal_match_type_summary.csv`, and `results/weho_public_reporting_note.md`.
- Saint Paul now has a local policy-timing package built from BPS county permits, QCEW county employment/wages, and FHFA Twin Cities MSA HPI with ordinance and amendment markers plus treated-versus-control county summaries.
- Mountain View now has a bounded public search pilot built from the live `mvrent-api.mountainview.gov` backend. The public auth bootstrap, bearer-token flow, search-count endpoint, and search-result endpoint are all verified and implemented; current sampled results include both covered-property `APN` rows and rent-registry `CASE` rows.
- Berkeley now has a bounded public registry pilot built from the live `rentregistry.cityofberkeley.info` guest search flow. The current pilot verifies address-keyed `ADDRESS` results and `APN` recovery, but deeper property-detail endpoints reject the guest token, so Berkeley is currently frozen at the public address/APN discovery layer rather than a lawful-rent panel.
- Santa Monica now has a bounded public MAR pilot built from the live city lookup page. The current saved pilot attempts `3` street queries and writes `9` returned rows with address, unit, MAR, tenancy date, bedroom count, and parcel fields; this is enough to treat Santa Monica as a real legal-ceiling pilot rather than only an audit. See `results/santa_monica_mar_pilot.md` and the `santa_monica_mar_*` tables.
- East Palo Alto now has a bounded public board-archive pilot built from the public site search and board-event pages. The current saved pilot writes `41` board-related search results plus `12` event-style pages with meeting dates, `iCal` links, one direct agenda link, and one canceled meeting flag. See `results/east_palo_alto_board_pilot.md` and the `east_palo_alto_board_*` tables.
- West Hollywood's original document-download blocker is now diagnosed precisely: the public PDF is behind Akamai WAF and the protected-stock runner only works today through the `--pdf-path` manual-download fallback. The second-stage minutes sample in this workspace was recovered through browser-backed downloads, not plain HTTP clients.
- Oakland now has a real local administrative-case package built from the public RAP search. The backend can fetch the search page, submit tenant-ground filters, post into individual case details, and parse case-progress histories without browser automation at runtime.
- Oakland now also has a near-full large detail layer on top of the RAP search universe. The current saved outputs keep the search universe complete for the selected grounds and expand the detailed case layer to `3,805` case rows (`69` code-violation rows, `3,000` decrease-in-services rows, and `736` fewer-housing-services rows). The rebuilt 311 linkage now reaches `387` matched request rows across `206` RAP addresses and `2,977` normalized RAP addresses, and the new `oakland_rap_coverage_summary.csv` makes search-versus-detail saturation explicit by ground.
- San Francisco now has a reporting-ready inventory backend from the official DataSF Rent Board Housing Inventory dataset. The current pull writes `533,205` unit-level rows across submission years `2022-2026` plus citywide, year, neighborhood, supervisor-district, year-by-neighborhood, year-by-district, rent-band, occupancy, and rollout summaries. The strongest current descriptive read is inventory scale and composition: `41` neighborhoods, `11` supervisor districts, median rent midpoint `2,375.5`, and `468,062` non-owner-occupied rows. See `results/sf_inventory_reporting_note.md` and the companion `sf_inventory_*` tables.
- San Jose now has a real descriptive policy-timing package built from BPS county permits, QCEW county employment/wages, and FHFA MSA HPI. The current package treats Santa Clara County as the local county proxy and San Mateo plus Contra Costa as Bay Area comparison counties, with outputs under `results/tables/san_jose_*` and `results/san_jose_policy_timing.md`.
- Oakland now also has a second-stage open-data linkage to official Oakland 311 code-enforcement requests. The current promoted linkage reaches `387` matched request rows across `206` RAP addresses on top of the `3,805`-row detailed RAP layer. This is now a serious local administrative panel rather than only a bounded sample. See `results/oakland_local_pilot.md` and the companion `oakland_*` tables.
- Oakland now also has a reusable admin-summary layer built from the saved RAP detail/progress files. New tables split RAP detail by filing year, progress activity, hearing officer, and yearly RAP-to-311 linkage counts, which makes Oakland useful for public reporting even without a near-full-detail panel.
- Oakland now also has a separate search-universe runner that writes `oakland_rap_search_universe_*` files without fetching case details. The full current universe is now landed: `code_violation` (`69` rows / `3` pages), `decrease_in_services` (`3,562` rows / `143` pages), and `fewer_housing_services` (`721` rows / `29` pages).
- Washington, DC now has a real local parcel-profile pilot built from the official `ITSPE` ArcGIS FeatureServer. The current full residential pull covers `130,934` parcels and uses `HSTDCODE` presence as an owner-occupied proxy versus blank `HSTDCODE` as a rental-probable proxy. This is a stock-composition / assessed-value design, not a bulk public rent-registry panel. See `results/dc_local_pilot.md` and the companion `dc_itspe_*` tables.
- Washington, DC now also has an official condo enrichment layer from the CAMA condo FeatureServer. The current `SSL`-keyed join matches `40,887` residential parcels to condo characteristics, which materially improves the usable parcel-characteristics surface for future DC local work.
- Washington, DC now also has a ward-comparison package built with no new fetches from the saved ITSPE and condo-enriched parcel files. The current outputs include ward rental-share tables, ward assessment-gap tables, condo ward/tenure summaries, and two public figures under `results/figures/dc_ward_*`.
- Washington, DC now also has a neighborhood-level disparity package from the same saved parcel files. The new outputs rank `56` neighborhoods by rental-versus-owner assessment-change disparity and add `results/tables/dc_nbhd_assessment_disparity.csv`, `results/figures/dc_nbhd_assessment_disparity.png`, and `results/dc_nbhd_disparity.md`.
